# onnxruntime-node 1.22 tries to fetch optional CUDA libraries on linux/x64 and
# looks them up under a Windows runtime path, so a plain install fails.
# CPU binaries are already in the package. Set ONNXRUNTIME_NODE_INSTALL to
# override this (for example cuda12) when that download is actually needed.
onnxruntime-node-install=skip
